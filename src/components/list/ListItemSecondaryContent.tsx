import React, { FC } from 'react';

export const ListItemSecondaryContent: FC = ({ ...props }) => {
    return <span className="mdl-list__item-secondary-content" {...props} />;
};

export default ListItemSecondaryContent;
