import React, { FC } from 'react';

export const ListItemAvatar: FC = ({ children }) => {
    return <i className="material-icons mdl-list__item-avatar">{children}</i>;
};

export default ListItemAvatar;
